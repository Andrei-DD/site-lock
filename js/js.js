const swiper = new Swiper('.swiper', {
    loop: true,
    mousewheel: true,
    speed: 1000,
    navigation: {
        nextEl: '.swiper-button-next',
        prevEl: '.swiper-button-prev',
    },
});

//console.log();